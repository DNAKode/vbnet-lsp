{
  "name": "codex-vscode-client-tests",
  "version": "0.1.0",
  "private": true,
  "description": "VS Code client harness for LSP validation tests.",
  "scripts": {
    "compile": "tsc -p . && tsc -p extension",
    "test": "npm run compile && node ./out/runTests.js"
  },
  "devDependencies": {
    "@types/glob": "^8.1.0",
    "@types/mocha": "^10.0.0",
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "@vscode/test-electron": "^2.4.0",
    "glob": "^10.3.0",
    "mocha": "^10.3.0",
    "typescript": "^5.4.0"
  }
}
